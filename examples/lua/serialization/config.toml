# Serialization example configuration
# Demonstrates json and yaml modules in Lua

# Mount point for the FUSE filesystem
mountpoint = "/tmp/nklhd-serialization"

# Lua script that defines routes (relative to this config file directory)
rootscript = "main.lua"

# Enable verbose debug logging
verbose = false

# Optional: maximum concurrent handlers
# max_handlers = 4